<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"

    app:startDestination="@id/todoListFragment">

    <fragment
        android:id="@+id/todoListFragment"
        android:name="com.example.todolist.TodoListFragment"
        android:label="todo_list_fragment"
        tools:layout="@layout/todo_list_fragment" >
        <action
            android:id="@+id/to_CreateListFragment"
            app:destination="@id/createListFragment" />
    </fragment>
    <fragment
        android:id="@+id/createListFragment"
        android:name="com.example.todolist.CreateListFragment"
        android:label="create_list"
        tools:layout="@layout/create_list" />


</navigation>